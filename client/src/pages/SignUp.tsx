import AuthForm from "../components/AuthForm";

export default function SignUp() {
    return (
        <AuthForm title='Create an account' miniTitle='Already have an account?' miniButton='Login' button='Sign Up' />
    )
}